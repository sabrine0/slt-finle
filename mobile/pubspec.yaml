name: swarm_mobile
description: "SWARM-Traffic AI — Copilote Cognitif B2C (Tomorrow Systems)."
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^14.6.2
  google_fonts: ^6.2.1
  google_maps_flutter: ^2.9.0
  geolocator: ^13.0.2
  shared_preferences: ^2.3.3
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2
  socket_io_client: ^2.0.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
